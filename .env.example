# Server-side SMTP configuration for the contact form
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_FROM_EMAIL=your-sender@example.com

# Every enquiry is delivered to the fixed address in app/api/contact/route.ts

GITHUB_TOKEN=